TEST-ONLY TLS FIXTURES

These files contain a public CA, certificate, and private key used only by the
hermetic Gears provider mock. They provide no secrecy and must never be used
for a production service. This directory is packaged only in motor-os-dev.img.
