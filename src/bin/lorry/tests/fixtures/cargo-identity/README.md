# Lorry Cargo identity fixture

This dependency-free package proves that Lorry and the paired Cargo produce
byte-identical native and Motor release artifacts. It is intentionally
unrelated to repository applications such as Red or Rush.
