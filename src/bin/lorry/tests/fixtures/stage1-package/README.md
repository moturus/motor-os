# Stage-1 test fixture

This dependency-free package is used only for Lorry's frozen Cargo identity
tests. It retains the historical `red` package and target names so those
identity vectors remain stable; it contains no code from the current editor.
