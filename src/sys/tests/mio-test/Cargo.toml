[package]
name = "mio-test"
version = "0.1.0"
edition = "2021"

[dependencies]
log = "0.4.8"
moto-rt = { path = "../../lib/moto-rt" }

# NOTE: mio is patched in the workspace Cargo.toml
mio = { version = "1.0.1", features = ["net", "os-poll"]}
